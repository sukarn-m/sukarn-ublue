name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          chmod +x tests/*.sh
          for t in tests/test_*.sh; do
            echo "Running $t..."
            ./"$t"
          done
