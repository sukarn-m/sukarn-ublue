modules:
  - type: rpm-ostree
    install:
# Terminal
      - byobu
      # - guake # Also requires the script configure-screen.sh
# Gaming
      # - mangohud
      # - gamemode.i686
      - gamescope
# Nextcloud. Also requires the script nextcloud-vfs.sh
      - nextcloud-client-devel # Required on Fedora for virtual files.
      - nextcloud-client-nautilus # The main nextcloud client.
# Miscellaneous
      - android-tools
      - simple-scan # Scanner
      - syncthing # File sync
      - libcupsfilters # For Canon LBP2900 printer
      - gtk2 # For ePass2003
      - gnome-boxes # Virtualization
# Gnome Themes
      - gnome-shell-theme-yaru
      - yaru-gtk2-theme
      - yaru-gtk3-theme
      - yaru-gtk4-theme
      - yaru-gtksourceview-theme
      - yaru-icon-theme
      - yaru-sound-theme
      - yaru-theme
    remove:
      ## Firefox has to be removed in 'main' image, but not in bluefin, bazzite, and aurora.
      - firefox
      - firefox-langpacks
