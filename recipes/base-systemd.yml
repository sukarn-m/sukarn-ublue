modules:
  - type: systemd
    system:
      enabled:
        - var-mnt-nas.automount
        - var-mnt-nas.mount
        - var-mnt-roms.automount
        - var-mnt-roms.mount
        - nas-monitor.timer
        - generate-ssh-certificate.service
      disabled:
        - tailscaled.service
